import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ["img.clerk.com"], // add any other domains your photos come from
	},
};

export default nextConfig;
